# Example Prompt: Article

Use page-to-md to convert `https://example.com/article` into Markdown.

Requirements:

- Save outputs under `browser-operator-runs/`.
- Preserve source URL and accessed time.
- Mark missing author or publish time as unknown.
- Include validation warnings.
